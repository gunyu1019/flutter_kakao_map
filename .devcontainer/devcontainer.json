// For format details, see https://aka.ms/devcontainer.json.
{
  "name": "Cloud-Devleopment-Environment(Flutter)",
  "build": {
    /* "args": {
      "VARIANT": "ubuntu-22.04",
      "WORKSPACE": "/workspaces/flutter_kakao_map",

      // Android-SDK version
      "COMMAND_LINE_TOOLS_VERSION": "11076708",
      "ANDROID_PLATFORM_VERSION": "35",
      "ANDROID_BUILD_TOOLS_VERSION": "35.0.2"
    }, */
    "dockerfile": "Dockerfile"
  },

  // Features to add to the dev container. More info: https://containers.dev/features.
  // "features": {},

  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  // "forwardPorts": [],

  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "dart-code.flutter",
        "dart-code.dart-code",
        "hzgood.dart-data-class-generator",

        // Android (Kotlin)
        "fwcd.kotlin",
        "vscjava.vscode-gradle",
        "vscjava.vscode-java-debug",

        // Github Codespace
        "github.vscode-github-actions",
        "ms-azuretools.vscode-docker"
      ]
    }
  },

  // Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
  "remoteUser": "codespace"
}