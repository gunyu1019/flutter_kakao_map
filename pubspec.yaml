name: flutter_kakaomaps
description: "A natvie based kakao maps plugin."
version: 0.1.0-dev.5
homepage: https://github.com/gunyu1019/flutter_kakao_maps

environment:
  sdk: ^3.5.3
  flutter: '>=3.3.0'

dependencies:
  collection: ^1.19.0
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        package: kr.yhs.flutter_kakaomaps
        pluginClass: FlutterKakaoMapsPlugin
