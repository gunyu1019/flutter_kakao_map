name: flutter_kakao_maps
description: "An unoffical kakao maps plugin."
version: 0.0.1
homepage: https://github.com/gunyu1019/flutter_kakao_maps
publisher: gunyu1019@gmail.com

environment:
  sdk: ^3.5.3
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        package: kr.yhs.flutter_kakao_maps
        pluginClass: FlutterKakaoMapsPlugin
